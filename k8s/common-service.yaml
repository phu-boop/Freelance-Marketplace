apiVersion: apps/v1
kind: Deployment
metadata:
  name: common-service
  namespace: freelance-marketplace
  labels:
    app: common-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: common-service
  template:
    metadata:
      labels:
        app: common-service
    spec:
      containers:
      - name: common-service
        image: user/freelance-common-service:latest
        ports:
        - containerPort: 3013
        env:
        - name: PORT
          value: "3013"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: db-secrets
              key: common-db-url
        - name: ALLOWED_ORIGINS
          valueFrom:
            configMapKeyRef:
              name: freelance-config
              key: ALLOWED_ORIGINS
        livenessProbe:
          httpGet:
            path: /health
            port: 3013
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3013
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: common-service
  namespace: freelance-marketplace
spec:
  selector:
    app: common-service
  ports:
  - port: 3013
    targetPort: 3013
  type: ClusterIP
